import React from 'react';


const ColorBackground = (colors) => {

    
    
    document.body.style =`background:${colors.colors}`;
    return (
        <></>
    );
};

export default ColorBackground;